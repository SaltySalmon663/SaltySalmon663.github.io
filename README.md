
  # Model Railroad FInaces (Community)

  This is a code bundle for Model Railroad FInaces (Community). The original project is available at https://www.figma.com/design/nDla6SBD46mNzoKy9I3dhI/Model-Railroad-FInaces--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  